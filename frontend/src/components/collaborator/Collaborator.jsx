function Collaborator({ initials }) {
    return (
        <span className="inline-flex items-center justify-center h-8 w-8 rounded-full bg-blue-100 text-sm font-medium text-blue-800 ring-2 ring-white">{initials}</span>
    )
}

export default Collaborator