function Collaborator({ name }) {
    return (
        <span className="inline-flex items-center justify-center h-8 w-8 rounded-full bg-blue-100 text-sm font-medium text-blue-800 ring-2 ring-white p-4">{name}</span>
    )
}

export default Collaborator